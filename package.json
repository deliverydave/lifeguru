{
  "name": "couples-coach",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "license": "UNLICENSED",
  "description": "Privacy-first couples coaching (M0 thin slice)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "dev:api": "npm run dev -w @couples-coach/api",
    "dev:web": "npm run dev -w @couples-coach/web"
  }
}
